pluginGroup = dev.reginaldomorais.claudedock
pluginVersion = 0.9.0

# A partir de 2025.3 (253) a JetBrains parou de publicar a Community Edition (IC)
# separadamente: a distribuição é unificada. Compilamos contra ela.
platformVersion = 2026.2

# Sem until-build (D-03): o plugin não é desabilitado a cada upgrade de IDE.
pluginSinceBuild = 252

org.gradle.jvmargs = -Xmx2g
org.gradle.caching = true
org.gradle.configuration-cache = true
kotlin.stdlib.default.dependency = false
